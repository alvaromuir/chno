<% title(@event.name, "Events") %>

<h2><%= @event.name %></h2>
<%= link_to "Edit Event", edit_event_path(@event) %>
<%= link_to "Delete Event", 
    event_path(@event),
    method: :delete,
    data: { confirm:
            "Are you sure you want to delete this event?"
          } %>

<%= link_to "New Project", new_event_project_path(@event) %>